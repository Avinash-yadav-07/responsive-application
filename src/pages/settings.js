import React from 'react';
import './settings.css';

const Settings = () => (
  <div className="settings">
    <h1 className="settings-title">Settings</h1>
    <p className="settings-description">Configure your application settings here.</p>
  </div>
);

export default Settings;